# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

강사 2인 (박환희, 김정화) — 하오팅 중국어 교습소 소속. 각자 학생을 담당하며 동시에 같은 데이터(학생, 상담, 매출, 회차)를 실시간으로 함께 보고 관리한다. PC와 모바일 브라우저 양쪽에서 접속한다. (Inferred: 강사가 서로의 담당 학생 데이터까지 자유롭게 열람/수정하는지, 운영 관리자 등 제3의 역할이 있는지는 코드로 확인되지 않음 — 확인 필요 시 추후 갱신.)

## Product Purpose

하오팅 중국어 학원의 학생 관리, 상담 기록, 회차(수업) 진행, 결제/재등록/환불, 매출 통계를 한 곳에서 처리하는 내부 운영 도구. 강사가 각 학생의 등록 정보·수업 일정·진도/숙제·결제 이력을 빠짐없이 기록하고, "오늘 수업 대상"과 "결제(재등록) 예정일"을 즉시 파악하는 것이 성공 기준.

## Positioning

범용 학원관리 SaaS가 아니라, 하오팅 중국어의 실제 운영 절차(2인 강사 체제, 회차 단위 결제 묶음, 재등록 이력 관리, 구글 캘린더/시트 연동, 안내 문자 자동 생성)에 맞춰 직접 만들어 쓰는 맞춤형 내부 도구.

## Operating Context

- 단일 페이지 앱(SPA). Firebase Firestore로 실시간 동기화되어 여러 강사가 동시에 같은 화면을 본다.
- 로그인: Firebase Authentication(이메일/비밀번호), 강사별 계정.
- 주요 화면: 학생 목록/상세(회차 관리 포함), 상담 기록, 매출 관리, 학습(진도/숙제, Google Sheets 연동), 환불 내역서(PDF), 구글 캘린더 자동 동기화.
- 수업 현장에서 모바일로 즉석 확인·조작하는 흐름이 잦다 (오늘 수업 대상 필터, 안내 문자 텍스트 복사 기능 존재).

## Capabilities and Constraints

- 학생 CRUD, 검색/필터/정렬, 수강 여부 토글, 학생 탭(그룹) 분류.
- 회차(세션) 단위 관리: 완료 체크, 날짜/시간, 휴강/취소 + 보강일, 구글 캘린더 이벤트 동기화, 안내 문자 자동 생성·복사.
- 결제 묶음(등록+재등록) 관리, 잔여 회차 기반 결제 예정일 추정, 환불 내역서 작성/PDF 다운로드.
- 상담 기록 CRUD, 매출/신규 등록 추이 통계.
- 기술 스택: 순수 HTML + Vanilla JS (`app.js` 단일 파일, 모듈 미사용) + Tailwind CSS, Firebase(Firestore/Auth), Vercel 배포.
- 제약: 빌드 도구·프레임워크 없이 단일 `app.js`로 전체 로직을 유지 — 리팩터링 범위가 클수록 회귀 위험이 크다. 기능/데이터 흐름을 바꾸지 않고 UI만 다듬는 작업이 안전하다.

## Brand Commitments

- 이름: 하오팅 중국어(중국어 교습소). 별도로 확정된 컬러 시스템·타이포그래피 가이드는 코드에서 확인되지 않음. `logo.jpg` 자산은 존재.

## Evidence on Hand

- README.md에 기능 명세와 데이터 모델이 문서화되어 있음.
- 실사용 데이터는 Firestore에 있고 저장소 코드에는 없음(앱 내 일부 시드 상수만 존재).
- 최근 커밋 이력(FINDING-001, 002, 004, 005)에 학생 행 키보드 접근성, 읽기전용 필드 시각 구분, 터치 타깃 크기, 모바일 표 스크롤 어포던스 개선이 이미 진행됨 — 유사한 사용성 개선이 이어지고 있는 프로젝트.

## Product Principles

1. 강사가 매일 실제로 쓰는 업무 절차(회차 체크, 재등록, 안내 문자 복사)를 그대로 따라가는 도구여야 한다 — 범용 기능보다 학원 운영 흐름이 우선.
2. 기능·데이터 정확성이 최우선이며, 리디자인은 기존 동작과 데이터 흐름을 반드시 보존해야 한다 (사용자가 명시적으로 요구).
3. PC와 모바일 양쪽에서 동일하게 잘 동작해야 한다 — 수업 현장 모바일 사용이 잦다.
4. 2인 소규모 운영 조직에 맞는 실용적 단순함 — 과도한 설정·온보딩이 필요 없다.

## Accessibility & Inclusion

특정 접근성 표준이 명시적으로 요구된 바는 없으나, 최근 커밋에서 키보드 접근성·터치 타깃 크기 등 접근성 개선이 실제로 진행 중임 — 이 흐름을 이어가는 것이 바람직하다.
