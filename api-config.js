/* ============================================================
 * 내부 API 보호용 토큰
 * ------------------------------------------------------------
 * Google Calendar / Sheets 연동을 처리하는 /api/* 서버리스 함수를
 * 호출할 때 이 토큰을 함께 보냅니다. Vercel 프로젝트 환경변수
 * INTERNAL_API_TOKEN 과 동일한 값으로 맞춰 주세요.
 *
 * ⚠️ 보안 안내:
 * - 이 파일은 브라우저에 그대로 노출되는 정적 JS이므로, 아래 값은
 *   "진짜 비밀"이 아닙니다. URL만 아는 임의의 요청/봇을 걸러내는
 *   수준의 장치이며, 진짜 인가는 Firebase Authentication 로그인으로
 *   보호되는 화면 안에서만 이 코드가 실행된다는 사실에 의존합니다.
 * - 값을 비워두면(빈 문자열) 헤더 없이 요청하며, 서버 쪽
 *   INTERNAL_API_TOKEN 도 설정하지 않았다면 정상 동작합니다.
 * ============================================================ */
window.HAOTING_API_TOKEN = "";
